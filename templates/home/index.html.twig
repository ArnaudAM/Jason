{% extends "base.html.twig" %}
{% block body %}
    <header>
        <h1>
        <img src="https://www.wildcodeschool.com/assets/logo_main-e4f3f744c8e717f1b7df3858dce55a86c63d4766d5d9a7f454250145f097c2fe.png" alt="Wild Code School logo" />
        Les Argonautes
        </h1>
    </header>
        <h2>Ajouter un(e) Argonaute</h2>
        <div class = "new-member-form">
        {{ include('argonautes/_form.html.twig') }}
        </div>
    <!-- Member list -->
    <h2>Présentation des membres</h2>
    <section class="member-list">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Softskill</th>
                </tr>
            </thead>
            <tbody>
            {% for argonaute in argonautes %}
                <tr>
                    <td>{{ argonaute.name }}</td>
                    <td>{{ argonaute.softskill }}</td>
{#                    <td>{{ include('argonautes/_delete_form.html.twig') }}</td>#}                </tr>
            {% else %}
                <tr>
                    <td colspan="4">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </section>
{% endblock %}
