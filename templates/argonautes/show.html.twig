{% extends 'base.html.twig' %}

{% block title %}Argonautes{% endblock %}

{% block body %}
    <h1>Argonautes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ argonaute.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ argonaute.name }}</td>
            </tr>
            <tr>
                <th>Softskill</th>
                <td>{{ argonaute.softskill }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('home_index') }}">back to home</a>

    <a href="{{ path('argonautes_edit', {'id': argonaute.id}) }}">edit</a>

    {{ include('argonautes/_delete_form.html.twig') }}
{% endblock %}
